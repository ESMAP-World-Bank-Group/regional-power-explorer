# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Data pipeline intermediates (generated, not committed)
data-source/cache/
data-source/*.csv
data-source/gem/
config/api_tokens.ini
.eptr2-tgt

# Raw source inputs — kept out of the public repo. Raw downloads (national
# reports, energy balances, API dumps…) belong in the private data store;
# only the processed data served by the app is committed.
# NOTE: AZE/GEO/TUR/EAPP were committed earlier and stay tracked — untracking
# them is a separate data-governance decision (see roadmap N2).
data-source/raw/

# Python
__pycache__/
*.pyc
*.pyo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Full internal project status (local only — public sanitized copy lives in public/)
docs/PROJECT_STATUS_internal.html
